<tr>
    <td class="header">
        <a href="{{ $url }}" style="display: inline-block;">
            @if (trim($slot) === 'header')
                <img src="{{ asset('assets/media/logos/logo.png') }}">
            @else
                <img src="{{ asset('assets/media/logos/logo.png') }}">
            @endif
        </a>
    </td>
</tr>
